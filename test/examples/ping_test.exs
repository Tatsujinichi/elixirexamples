defmodule Examples.Ping_pong_test do
  @moduledoc false
  use ExUnit.Case, async: true
  import ExUnit.CaptureIO
  alias Examples.Ping
q
  test "test ping send" do

  end

  test "test ping receive" do

  end

  test "test io" do
    assert capture_io()
  end

end
